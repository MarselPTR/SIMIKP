import { db } from "./src/db"; import { activities, assignments } from "./src/db/schema/activities"; import { eq, and } from "drizzle-orm"; async function run() { const tDate = new Date("2026-09-21T00:00:00.000Z"); const res = await db.select({ id: assignments.id, title: activities.title, date: activities.activityDate }).from(assignments).innerJoin(activities, eq(assignments.activityId, activities.id)).where(eq(activities.activityDate, tDate)); console.log("Result:", res); process.exit(0); } run();
