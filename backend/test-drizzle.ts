import { db } from "./src/db"; import { activities } from "./src/db/schema/activities"; import { eq, and } from "drizzle-orm"; async function run() { const tDate = new Date("2026-09-21T00:00:00.000Z"); const res = await db.select({ title: activities.title, date: activities.activityDate }).from(activities).where(eq(activities.activityDate, tDate)); console.log("Result for tDate:", tDate, "->", res); process.exit(0); } run();
